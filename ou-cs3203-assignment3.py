def calculateSum(numbers):
    sum = 0
    for x in numbers:
        sum = sum + x
    return sum

def calculateProduct(numbers):
    product = 0
    for x in numbers:
        product = product * x
    return product
